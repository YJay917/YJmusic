# 项目设计余俊20240984 - Vue3重构

这是从 `D:\Frontend\Javascipt\YJay\实训项目\项目设计余俊20240984` 复制出来的 Vue3 承载版本，原项目目录未被修改。

使用 npm 启动后预览：

```bash
npm install
npm run dev
```

原页面完整副本在 `public/exact`。

当前优化：
- 使用 Vite + Vue3 单文件组件结构。
- `src/main.js` 挂载 `src/App.vue`，`src/router/index.js` 管理首页路由。
- 原 HTML、CSS、JS、图片、音乐、iframe 弹窗、登录、播放条和动态效果按原项目运行。
- 不做分页改造，尽量保持一比一还原。
